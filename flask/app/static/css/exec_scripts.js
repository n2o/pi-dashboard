// Generated by CoffeeScript 1.8.0
(function() {
  var change_process_label, icon_play, icon_spinner;

  icon_spinner = "<i class='fa fa-refresh fa-spin'>";

  icon_play = "<i class='fa fa-play'>";

  $(function() {
    return $(".execute").click(function() {
      var button, script, url;
      $(".execute").prop("disabled", true);
      button = $(this);
      button.context.innerHTML = icon_spinner;
      change_process_label("warning");
      script = $(this).context.title;
      url = "/stream/" + script;
      return $("#execute").attr("src", url);
    });
  });

  change_process_label = function(status) {
    if (status === "success") {
      return $("#process_label")[0].innerHTML = "<span class='label label-success'>Bereit</span>";
    } else if (status === "warning") {
      return $("#process_label")[0].innerHTML = "<span class='label label-warning'>Wird ausgeführt</span>";
    } else if (status === "danger") {
      return $("#process_label")[0].innerHTML = "<span class='label label-danger'>Fehler</span>";
    }
  };

  window.stream_finished = function() {

    /* This function is triggered when the streaming process has finished */
    var button, buttons, _fn, _i, _len;
    buttons = $(".execute");
    buttons.prop("disabled", false);
    _fn = function() {
      return button.innerHTML = icon_play;
    };
    for (_i = 0, _len = buttons.length; _i < _len; _i++) {
      button = buttons[_i];
      _fn();
    }
    return change_process_label("success");
  };

}).call(this);

//# sourceMappingURL=exec_scripts.js.map
